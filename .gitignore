# WordPress to Static Site Automation
# Ignore temporary files and build artifacts

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
wordpress_spellcheck_env/
venv/
env/

# Temporary build outputs (except the final public/ directory)
static-output/
static-test-output/
temp/
build/

# System files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log
deploy.log

# Spell check reports
spelling_check_report.md

# Spell check timestamp (tracked in repo)
!.last_spell_check_timestamp

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Gitleaks secret scanning reports
gitleaks-report.json
results.sarif

# Lighthouse CI reports (but keep config files)
.lighthouseci/
lighthouse-*.html
lhr-*.json
manifest.json
assertion-results.json

# Image optimization results
optimization-results.json

# Build cache (optional - can be tracked for incremental CI/CD builds)
# Comment out the line below to enable cache tracking in git
.build-cache.json

# Keep the public/ directory (this is what Cloudflare deploys)
!public/
docs/IMAGE_CACHE_DIAGNOSIS.md
