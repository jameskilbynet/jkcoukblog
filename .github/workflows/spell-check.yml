name: Spell Check WordPress Content

on:
  workflow_dispatch:      # Manual trigger
    inputs:
      post_count:
        description: 'Number of posts to check (ignored if check_modified is true)'
        required: false
        default: '5'
      check_modified:
        description: 'Only check posts modified since last run'
        required: false
        default: 'false'
        type: boolean

jobs:
  spell-check:
    runs-on: self-hosted  # Self-hosted to access WordPress behind Cloudflare Access
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        
    - name: Install dependencies
      run: |
        echo "🔧 Installing Python dependencies..."
        pip install requests beautifulsoup4
        echo "✅ Dependencies installed"
        
    - name: Run spell check
      env:
        WP_AUTH_TOKEN: ${{ secrets.WP_AUTH_TOKEN }}
        OLLAMA_API_CREDENTIALS: ${{ secrets.OLLAMA_API_CREDENTIALS }}
        WP_URL: https://wordpress.jameskilby.cloud
        OLLAMA_URL: https://ollama.jameskilby.cloud
        OLLAMA_MODEL: llama3.1:8b
      run: |
        echo "🚀 Starting spell check..."
        
        CHECK_MODIFIED="${{ github.event.inputs.check_modified || 'false' }}"
        TIMESTAMP_FILE=".last_spell_check_timestamp"
        
        if [ "$CHECK_MODIFIED" = "true" ] && [ -f "$TIMESTAMP_FILE" ]; then
          LAST_CHECK=$(cat "$TIMESTAMP_FILE")
          echo "📅 Checking posts modified since: $LAST_CHECK"
          export SINCE_TIMESTAMP="$LAST_CHECK"
          
          # Run spell checker for modified posts only
          ./ollama_spell_checker.py || SPELL_CHECK_FAILED=1
          
          # Update timestamp after successful check
          date -u +"%Y-%m-%dT%H:%M:%SZ" > "$TIMESTAMP_FILE"
        else
          POST_COUNT="${{ github.event.inputs.post_count || '5' }}"
          echo "📝 Checking $POST_COUNT most recent posts"
          
          # Run spell checker (will exit 1 if errors found)
          ./ollama_spell_checker.py $POST_COUNT || SPELL_CHECK_FAILED=1
        fi
        
        # Save exit code for later steps
        if [ "$SPELL_CHECK_FAILED" = "1" ]; then
          echo "SPELL_CHECK_STATUS=failed" >> $GITHUB_ENV
          echo "⚠️  Spelling errors detected"
        else
          echo "SPELL_CHECK_STATUS=passed" >> $GITHUB_ENV
          echo "✅ No spelling errors found"
        fi
        
    - name: Upload spell check report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: spelling-report-${{ github.run_id }}
        path: spelling_check_report.md
        retention-days: 7
        
    - name: Comment on commit if errors found
      if: env.SPELL_CHECK_STATUS == 'failed' && github.event_name == 'workflow_dispatch'
      run: |
        echo "📝 Spelling errors found. Report uploaded as artifact."
        
    - name: Notify Slack on success
      if: env.SPELL_CHECK_STATUS == 'passed'
      uses: 8398a7/action-slack@v3
      with:
        status: custom
        channel: '#web'
        custom_payload: |
          {
            "text": "✅ WordPress Spell Check Passed",
            "attachments": [{
              "color": "good",
              "text": "All posts are clean!\n\n📝 Posts checked: ${{ github.event.inputs.post_count || '5' }}\n🔗 Report: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
            }]
          }
      env:
        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        
    - name: Notify Slack on errors
      if: env.SPELL_CHECK_STATUS == 'failed'
      uses: 8398a7/action-slack@v3
      with:
        status: custom
        channel: '#web'
        custom_payload: |
          {
            "text": "⚠️  WordPress Spell Check - Issues Found",
            "attachments": [{
              "color": "warning",
              "text": "Spelling errors detected\n\n📝 Posts checked: ${{ github.event.inputs.post_count || '5' }}\n🔗 Report: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\n\nPlease review the report and fix errors before publishing."
            }]
          }
      env:
        SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
